You are parsing a Mewgenics cat screenshot.

Return ONLY a valid JSON object. Do not return markdown. Do not return prose.

Use this exact schema:
{
  "cat": "",
  "gender": "?",
  "breedWith": "",
  "stats": {
    "str": "7",
    "dex": "7",
    "health": "7",
    "int": "7",
    "move": "7",
    "char": "7",
    "luck": "7"
  },
  "mutations": {
    "body": "",
    "head": "",
    "tail": "",
    "leg1": "",
    "leg2": "",
    "arm1": "",
    "arm2": "",
    "eye1": "",
    "eye2": "",
    "eyebrow1": "",
    "eyebrow2": "",
    "ear1": "",
    "ear2": "",
    "mouth": "",
    "fur": ""
  }
}

Rules:

BreedWith should default to the opposite sex:
- M cats -> F
- F cats -> M
- ? cats -> ANY

If there is a rainbow icon beside the cat level / age area:
- for M or F cats, set BreedWith to "?"
- for ? cats, keep BreedWith as "ANY"
- do not infer M or F in that case

BreedWith meanings:
- M = can only breed with M cats
- F = can only breed with F cats
- ? = can only breed with ? cats
- ANY = can breed with any gender

Gender should be "M" or "F" for male or female. Some cats are "?" and it should be set to "?".

Stats:
- Look at the two numbers for each stat.
- Output the RIGHT number exactly as a value from 1-7.
- Do NOT infer stats by row order alone. Identify each stat by its icon.
- The stat icons map as follows:
  - flexed arm icon = Strength = str
  - bow-and-arrow icon = Dexterity = dex
  - heart/medical cross icon = Constitution = health
  - lightbulb icon = Intelligence = int
  - boot with motion lines icon = Speed = move
  - lips icon = Charisma = char
  - clover icon = Luck = luck
- Constitution means Health.
- Dexterity means Dex.
- Speed means Move.
- If an icon is small, partially cut off, or blurry, use the icon shape and surrounding numbers. Do not swap health/dex/move/char/luck because of approximate row position.

Examples:
- if the right number is 7 -> output 7
- if the right number is 5 -> output 5

The planner logic later treats 7 as complete and anything else as incomplete, but you should still return the actual visible 1-7 value.

Mutation slots correspond to body parts.

Convert mutation descriptions following the example formats below.

mutation DICTIONARY:

+2 Char, -1 Luck -> +2char-1luck
+2 Dex, -1 Health -> +2dex-1health
+2 Move, -1 Dex -> +2move-1dex
+2 Move, -1 Luck -> +2move-1luck
+2 Health -> +1health
+2 Dex -> +1dex
+2 Move -> +1move
+1 Range -> +1range
+2 Int, -1 Str -> +2int-1str
+2 Str, -1 Dex -> +2str-1dex
+2 Move, -1 Str -> +2move-1str
+2 Health, -1 Char -> +2health-1char

Inflict Fear on contact -> fearOnContact

Units contacting you knocked back -> knockback
Start with backflips -> backflips
Gain move per turn -> gainMovePerTurn
Jump movement -> jumpMove
Tile immunity -> tileImmunity
Spawn coin -> spawnCoin
Inflict random debuff -> randomDebuff
Confuse enemy -> confusion
Reflect damage -> reflect
Bleed -> bleed
Burn -> burn
Leech -> leech

After translating, append the body part:

Examples:

fearOnContact(head)
knockback(tail)
jumpMove(arm)
+2char-1luck(eye)
+1range(eyebrow)
burn(ear)
bleed(mouth)

Named mutations should keep the expected token format exactly, for example:
+1health
+1dex
+1move
+1range
fearOnContact
knockback
backflips
gainMovePerTurn
jumpMove
tileImmunity
spawnCoin
randomDebuff
confusion
reflect
bleed
burn
leech

Stat-tradeoff mutations should use the exact lowercase format:
+2stat-1stat

Never output:
+2Char
-1Str
+2Char,-1Luck
InflictRandomDebuff
IncreaseCharDecreaseLuck

Always convert stat modifiers into the format:

+2stat-1stat

Examples:

+2 Char, -1 Luck -> +2char-1luck
+2 Dex, -1 Health -> +2dex-1health
+2 Move, -1 Dex -> +2move-1dex
+2 Move, -1 Luck -> +2move-1luck

If a slot is empty -> return an empty string for that JSON field.

Mutations labeled "Defect" should NOT be included.

For mutations not in this dictionary, follow the same format.

Mutation slot rules:
- body -> body
- head -> head
- tail -> tail
- first leg slot -> leg1
- second leg slot -> leg2
- first arm slot -> arm1
- second arm slot -> arm2
- first eye slot -> eye1
- second eye slot -> eye2
- first eyebrow slot -> eyebrow1
- second eyebrow slot -> eyebrow2
- first ear slot -> ear1
- second ear slot -> ear2
- mouth -> mouth
- fur -> fur

If a body part appears twice in the screenshot (2 legs, 2 arms, 2 eyes, 2 eyebrows), put the mutation in the left-most matching JSON field first and leave the second one empty unless a second separate mutation is visibly present.
